import React from 'react';
import { View, Text } from 'react-native';

const Random = () => {
    return (
        <View>
            <Text>Random</Text>
        </View>
    );
};

export default Random;
